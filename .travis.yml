language: java

jdk:
  - openjdk8

branches:
  only:
    - master
    - /v\d+.\d+.\d+/

before_deploy:
  - mvn compile war:war

deploy:
  provider: releases
  github_token: $GITHUB_TOKEN
  file: target/*.war
  skip_cleanup: true
  on:
    repo: michaelneu/gorillamail
    tags: true
